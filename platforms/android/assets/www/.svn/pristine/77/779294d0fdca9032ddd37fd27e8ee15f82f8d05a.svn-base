function url(req){
	return "http://localhost:8080/world-web/s/m?r="+req ;
}
function surl(req){


	return "http://localhost:8080/world-web/s/m?r="+req ;
}

function mi_exit(ind){
	//goBackView();
	window.history.go(ind);
}

function getValue(id){
	var _value = $(id).val();
	return trim(_value);
}
 //去左空格;   
function ltrim(s){     
    return s.replace(/(^\s*)/, "");  
 }   
 //去右空格;   
function rtrim(s){   
  return s.replace(/(\s*$)/, "");  
}   
 //去左右空格;   
 function trim(s){  
  return rtrim(ltrim(s));   
 }
 function checktoken(data){
	 if(data.loge == 0){
		 return true;
	 }else if(data.loge == 1){
		 window.location.href="login.html";
		 return false;
	 }else if(data.loge == 2){	 
		 window.location.href="login.html";
		 return false;
	 }
	 
 }
 
